# Bug reports / feedback need NO configuration: the report panel posts straight
# to FormSubmit (app/components/SiteFooter.tsx -> FORM_ENDPOINT), which emails the
# project inbox. There is no API key and no server route to set up — the
# recipient address just has to click FormSubmit's one-off "Activate Form" link
# the first time a report is submitted. See README -> Bug reports / feedback.

# Storage for the shared "anonymous love" counter (app/api/hearts/route.ts) —
# this is what makes the heart count grow with every visitor instead of stopping
# at whatever one device has seen.
#
# 1. Create a free Redis database: https://upstash.com (or Vercel -> Storage).
# 2. Copy the REST URL and the **write** token, both from that same database.
#    The read-only token cannot count hearts, and mixing a URL from one
#    integration with a token from another answers 401 Unauthorized.
# 3. Set them in Vercel -> Settings -> Environment Variables for Production AND
#    Preview, then REDEPLOY — env changes only apply to new deployments.
#
# Local dev: put the same two lines in .env.local (copy this file).
# Check the setup at any time:  npm run hearts:check -- --url <your site>
KV_REST_API_URL=
KV_REST_API_TOKEN=
# Upstash's own variable names work too:
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
